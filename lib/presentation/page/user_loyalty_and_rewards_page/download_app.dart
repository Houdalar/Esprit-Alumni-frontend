import 'package:flutter/material.dart';

class DownloadApp extends StatelessWidget {
  const DownloadApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Availble Soon'),
    );
  }
}
